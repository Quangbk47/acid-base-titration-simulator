// Independent CHEM-03 reference records.
// This fixture intentionally contains no import from src/chemistry or production code.
// pH methods: weak-acid quadratic at initial state, Henderson-Hasselbalch only in
// the buffer region, acetate hydrolysis at equivalence, and excess-OH stoichiometry
// after equivalence. Values were calculated from the stated input and 25 °C Kw.

const baseInput = Object.freeze({
  Ca: 0.1,
  Va: 0.025,
  Cb: 0.1,
  Ka: 1.8e-5,
  temperature: 298.15,
});

export const chem03Reference = Object.freeze([
  Object.freeze({
    id: 'initial',
    region: 'initial',
    input: Object.freeze({ ...baseInput, Vb: 0 }),
    volumeMl: 0,
    expectedVeqMl: 25,
    expectedPH: 2.87528,
    expectedStage: 'before-equivalence',
    expectedExcessSpecies: 'HA',
    pHTolerance: 0.005,
    veqToleranceMl: 1e-12,
    method: 'weak-acid dissociation quadratic',
  }),
  Object.freeze({
    id: 'buffer-20-percent',
    region: 'buffer',
    input: Object.freeze({ ...baseInput, Vb: 0.005 }),
    volumeMl: 5,
    expectedVeqMl: 25,
    expectedPH: 4.14267,
    expectedStage: 'before-equivalence',
    expectedExcessSpecies: 'HA',
    pHTolerance: 0.005,
    veqToleranceMl: 1e-12,
    method: 'Henderson-Hasselbalch with stoichiometric HA/A− moles',
  }),
  Object.freeze({
    id: 'half-equivalence',
    region: 'half-equivalence',
    input: Object.freeze({ ...baseInput, Vb: 0.0125 }),
    volumeMl: 12.5,
    expectedVeqMl: 25,
    expectedPH: 4.74473,
    expectedStage: 'before-equivalence',
    expectedExcessSpecies: 'HA',
    pHTolerance: 0.005,
    veqToleranceMl: 1e-12,
    method: 'Henderson-Hasselbalch; pH is pKa at equal HA/A− moles',
  }),
  Object.freeze({
    id: 'buffer-90-percent',
    region: 'buffer',
    input: Object.freeze({ ...baseInput, Vb: 0.0225 }),
    volumeMl: 22.5,
    expectedVeqMl: 25,
    expectedPH: 5.69897,
    expectedStage: 'before-equivalence',
    expectedExcessSpecies: 'HA',
    pHTolerance: 0.005,
    veqToleranceMl: 1e-12,
    method: 'Henderson-Hasselbalch with stoichiometric HA/A− moles',
  }),
  Object.freeze({
    id: 'pre-equivalence-99-percent',
    region: 'immediately-before-equivalence',
    input: Object.freeze({ ...baseInput, Vb: 0.02475 }),
    volumeMl: 24.75,
    expectedVeqMl: 25,
    expectedPH: 6.74036,
    expectedStage: 'before-equivalence',
    expectedExcessSpecies: 'HA',
    pHTolerance: 0.005,
    veqToleranceMl: 1e-12,
    method: 'Henderson-Hasselbalch at 99% neutralization',
  }),
  Object.freeze({
    id: 'equivalence',
    region: 'equivalence',
    input: Object.freeze({ ...baseInput, Vb: 0.025 }),
    volumeMl: 25,
    expectedVeqMl: 25,
    expectedPH: 8.72183,
    expectedStage: 'at-equivalence',
    expectedExcessSpecies: null,
    pHTolerance: 0.005,
    veqToleranceMl: 1e-12,
    method: 'acetate hydrolysis quadratic with Kb = Kw/Ka',
  }),
  Object.freeze({
    id: 'post-equivalence-101-percent',
    region: 'immediately-after-equivalence',
    input: Object.freeze({ ...baseInput, Vb: 0.02525 }),
    volumeMl: 25.25,
    expectedVeqMl: 25,
    expectedPH: 10.69680,
    expectedStage: 'after-equivalence',
    expectedExcessSpecies: 'OH⁻',
    pHTolerance: 0.005,
    veqToleranceMl: 1e-12,
    method: 'excess strong-base stoichiometry after dilution',
  }),
  Object.freeze({
    id: 'large-excess',
    region: 'post-equivalence-excess-base',
    input: Object.freeze({ ...baseInput, Vb: 0.05 }),
    volumeMl: 50,
    expectedVeqMl: 25,
    expectedPH: 12.52288,
    expectedStage: 'after-equivalence',
    expectedExcessSpecies: 'OH⁻',
    pHTolerance: 0.005,
    veqToleranceMl: 1e-12,
    method: 'excess strong-base stoichiometry after dilution',
  }),
]);

export const CHEM03_REFERENCE_CONVENTION = Object.freeze({
  source: 'independent analytical derivation from CHEMISTRY_MODEL assumptions',
  kw: 1e-14,
  pHToleranceBasis: '0.005 pH units; rounded reference values and region method',
  veqToleranceBasis: '1e-12 mL; exact stoichiometric Ca×Va/Cb result',
});
