// The baseline exposes state shape only; it does not simulate drops or calculate pH.
export const baselineState = Object.freeze({
  screen: 'idle',
  addedVolumeMl: null,
  hasSimulationData: false,
});

